import React from 'react';
import PropTypes from 'prop-types';

const Home = () => (
 <div className='h1' data-testid="Home">
    Welcome to CarBid
 </div>
);

Home.propTypes = {};

Home.defaultProps = {};

export default Home;
